---
description: Fluxo Git e ambientes (feature → preview → main)
alwaysApply: true
---

# Fluxo de desenvolvimento (Builders Club)

## Branches

```
feature/<id-ou-descricao>  →  PR →  feature/preview  →  PR →  main
```

| Branch | Papel |
|--------|--------|
| `feature/*` | Desenvolvimento |
| `feature/preview` | Staging / Preview Vercel |
| `main` | Produção |

## Regras

- **Sempre** criar `feature/FXXX-…` a partir de `feature/preview`. Não desenvolver em `feature/preview` nem em `main`.
- Ao concluir: commit → PR para `feature/preview` → **merge do PR** → `npm run db:migrate:staging` (só HML).
- Não abrir PR direto de `feature/*` para `main` como caminho padrão.
- Commits e PRs referenciam o ID da feature (`F00X`) quando houver.
- Preview usa envs de Preview (Neon HML, `BETTER_AUTH_URL` de staging).
- Produção (`main` + `db:migrate:prod -- --confirm`) **só com pedido explícito** do usuário.

## Migrations

Espelham o fluxo de branch:

| Momento | Comando | Banco |
|---------|---------|--------|
| Feature / merge em `feature/preview` | `npm run db:migrate:staging` | Neon HML |
| Após merge em `main` + confirmação | `npm run db:migrate:prod -- --confirm` | Neon prod |

- Features em andamento: **somente** HML (`db:migrate:staging`).
- **Nunca** migrar produção sem pedido explícito após validar no Preview.
- Produção: só depois do merge em `main` **e** confirmação do usuário.

## Domínios

- Produção: `https://comunidade-builders-club.devemdobro.com`
- Homologação: `https://hml-comunidade-builders-club.devemdobro.com`

`builders-club.devemdobro.com` (sem o `comunidade-`) é outra coisa: landing
estática, 404 em toda rota deste app. Não usar em link, env ou webhook.

Detalhes de envs Vercel: `docs/deploy-vercel.md`
